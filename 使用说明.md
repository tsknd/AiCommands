# Claude Code 会话工具 · 使用说明

> 日常用法速查。安装见 `安装指南.md`。

## 命令一览

| 命令 | 作用 |
|------|------|
| `lss` | 列出当前项目全部会话(按日期升序,旧在前,带标题) |
| `lss 关键词` | 搜**标题 + session-id**(快,默认) |
| `lss -f 关键词` | 搜**标题 + 正文全文**(慢但全,找聊过某话题的会话) |
| `lss --all` | 列出**所有项目** |
| `lss --all -f 关键词` | 所有项目 + 正文全文搜索 |
| `oss <id前缀>` | 按 id 前缀打开会话(唯一即开,自动唤起 VSCode) |
| `oss --all <id>` | 跨项目打开 |

## 典型流程

```bash
# 1) 找会话:先按标题搜,搜不到再 -f 搜正文
lss 地上铁              # 只搜标题 → 2 个
lss -f 地上铁           # 搜正文   → 21 个(凡 jsonl 提到"地上铁"的)

# 2) 记住目标 session-id 前几位(如 2ea2d18b)

# 3) 打开
oss 2ea2d18b            # 前缀唯一即开
```

> 列表里的 session-id 在 VSCode 终端 / Windows Terminal 里可以 **Ctrl+点击**直接打开,不一定要用 `oss`。

## 三个要点

1. **`lss`(默认)只搜标题,`lss -f` 搜正文全文。**
   标题是 AI 起的概括名(如"改造案件同步定时任务"),不一定含你记得的关键词(如"地上铁")。按标题搜不到时,加 `-f` 搜正文。正文搜索稍慢(要读整个 jsonl)。

2. **`oss` 前缀只要在范围内唯一即可**,不用敲完整 UUID。
   - 唯一 → 直接打开
   - 多个 → 列出全部完整 id,加长前缀再试
   - 无匹配 → 提示用 `lss` 查看

3. **打开的会话须属于当前 VSCode 工作区。**
   工作区是 xfawu,就只能开 xfawu 的会话;要开别的项目的,先把 VSCode 工作区切过去,或 `oss --all <id>`。

## 标题怎么来的

显示优先级:**用户手改标题 > AI 生成标题 > 首条消息**。所以你手改过标题的会话,显示的是你起的名字。

## 排错

| 现象 | 原因 / 处理 |
|---|---|
| 改完 profile 不生效 | 新开终端,或 `. $PROFILE`(PS)/ `source ~/.bashrc`(bash) |
| `oss` 唤不起 VSCode | 脚本会打印完整 URI,贴进 Win+R 运行框手动开 |
| 搜不到明明聊过的话题 | 关键词在正文,用 `lss -f 关键词` |
| 打开会话报 not found | 会话不属于当前工作区,切工作区或 `oss --all` |
| 表头还是旧的 aiTitle | profile 没重载,新开 pwsh 窗口 |
